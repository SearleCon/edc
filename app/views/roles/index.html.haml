%h2 Roles

- bootstrap_table headers:['Name', 'Number Of Users', 'Created', ''], types: [:bordered, :striped, :condensed] do
  - @roles.each do |role|
    %tr[role]
      %td= role.name
      %td= role.users.size
      %td= role.created_at.to_s(:default)
      %td= link_to "Delete", role_path(role),class: 'btn btn-danger',method: :delete

=link_to "Add role", new_role_path, class: 'btn btn-primary'