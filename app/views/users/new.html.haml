.page-header
  %h2
    #{fa_icon(:user)} New User

= bootstrap_form_for @user, url: users_path do |f|
  = f.alert_message "Please fix the errors below:"
  = f.text_field :name
  = f.email_field :email
  = f.collection_select :role_id, Role.all, :id, :name, prompt: true
  = f.primary 'Create'


